# Upload as: /home/finance.skytechsl.com/public_html/sky_erp/.htaccess
# (included in dist/ when you run npm run build — copy from public/.htaccess)

<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteRule ^backend/ - [L]
  RewriteRule ^assets/ - [L]

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ /sky_erp/index.html [L,QSA]
</IfModule>
